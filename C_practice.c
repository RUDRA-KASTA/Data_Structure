//////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
// 1. Find the largest number among the three numbers.      // ternary operator concept
//////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

/*
#include <stdio.h>

int num1,num2,num3,output;

int main(void)
{
    printf("enter the first number");
    scanf("%d",&num1);

    printf("enter the second number");
    scanf("%d",&num2);

    printf("enter the third number");
    scanf("%d",&num3);

    output = (num1>num2) ? ((num1>num3)?(num1):((num2>num3)?(num2):(num3))) : ((num2>num3) ? (num2):(num3));
    printf("largest of 3 number is : %d",output);

    return 0;
}
*/

//////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
// Example 2: C Program to check whether a year is a leap year using ternary operator
//////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

/*
#include <stdio.h>

int year,output;

int main(void)
{
    printf("enter the year");
    scanf("%d",&year);

    output = ((year%4 == 0 && year%100 != 0) || year % 400 == 0) ? (printf("leap year")) : (printf("not a leap year"));

    return 0;
}

*/


//////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
// 3. Write a Program to check whether a number is prime or not.
//////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

/*
#include <stdio.h>
int a,flag=0;

int main()
{
    printf("Enter the no.");
    scanf("%d",&a);

    if(a<=1)
        flag=1;
    else
    {
        for(int r=2 ; r<=a/2 ; r++)
        {
            if( a % r == 0)
               flag=1;
               break;
        }
    }

    if(flag)
        printf("%d not a prime no.",a);
    else 
        printf("%d is a prime no.",a);
}

*/

//////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
// 4. Write a Program to exchange the 2 variable without 3rd variable.
//////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

/*
#include <stdio.h>
int a,b;

int main()
{
    printf("Enter the 1 no.");
    scanf("%d",&a);

    printf("Enter the 2 no.");
    scanf("%d",&b);

    //a=a^b;
    //b=a^b;
    //a=a^b;

    a=a+b;
    b=a-b;
    a=a-b;

    printf("a:%d and b:%d", a,b);

    return 0;
}

*/

//////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
// 5. Write a Program to Replace all 0’s with 1’s in a Number.
//////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
/*
#include <stdio.h>
#include <math.h>
int a,num,cnt=0;

int main()
{
    printf("enter the value: ");
    scanf("%ld",&num);
    a=num;

    while(a > 0)
    {
        if((a%10) == 0)
            num += pow(10,(cnt));

        cnt++;
        a=a/10;
    }
        
    printf("%ld",num);
}
*/



//////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
// 7. Write a C program to print the Fibonacci series using recursion and without using recursion
//////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

/*
#include <stdio.h>

int fibonacci_series_with_recursion(int cnt,int first, int second)
{   
    int n = cnt;
    if(n >= 0)
    {
        int third = first + second;
        first = second;
        second = third;
        printf("%d ",third);
        fibonacci_series_with_recursion(n-1,first,second);
    }
}


int main()
{
    fibonacci_series_with_recursion(10,0,1);
}

*/


//////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
// 8. Write a program to check whether a string is a palindrome or not.
//////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

/*
#include <string.h>
#include <stdio.h>

void palidrom_check(char a[])
{
    int size = strlen(a);
    char *start = a;
    char *end = a + (size-1);
    int not=0;
    
    while(end>start)
    {
        if(*start != *end)
        {
            printf("not palindrome");
            return;
        }
        start++;
        end--;
    }
        printf("palindrome");
}

int main()
{
    palidrom_check("ba");
}
*/


//////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
// 9. Write a program to print the factorial of a given number with the help of recursion.
//////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////


/*
#include <stdio.h>

int factorial(int x)
{
    if(x > 0)
        return x*factorial(x-1);
    else 
        return 1;
}

int main()
{
    printf("%d",factorial(10));
}
*/

//////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
// 10. Write a program to check an Armstrong number.     // 153 = 1^3 + 5^3 + 3^3.
//////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

/*
#include <stdio.h>

long int armstrong_num(long int a,int po)
{
    if(a>0)
    {
        int temp = a%10;
        return pow(temp,po) + armstrong_num(a/10,po);
    }
    return 0;
}

int main()
{
    int sum = armstrong_num(153,3);
    printf("%d",sum);
    return 0;
}

*/

//////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
// 11.Write a program to reverse a given number.
//////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

/*
#include <stdio.h>
#include <math.h>

void reverse_num(int x)
{
    int result=0,temp;
    while(x>0)
    {
        temp = x % 10;
        result = result*10 + temp;
        x = x/10;
    }
    printf("%d",result);
}

int main()
{
    int x =46307;
    reverse_num(x);
}

*/


//////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
// 12. prove by coding that Stack is growing downwards
//////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

/*
#include <stdio.h>
#include <stdlib.h>
#include <string.h>

void function(int *p)
{
    int b;
    if((&p)>(&b))
    {
        printf("Stack is growing downwards\n");
    }
    else
    {
        printf("Stack is growing upwards\n");
    }
}

int main(void)
{
    int *a = NULL;
    function(a);
    return 0;
}

*/

//////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
// 13. Write a Program to convert the binary number into a decimal number.
//////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

/*

#include <stdio.h>
#include <math.h>

int main()
{
    int bn=0,cnt=0,result=0;
    printf("Enter the binary number.");
    scanf("%d",&bn);

    while(bn!=0)
    {
        result += (bn%10)*pow(2,cnt);
        bn = bn / 10;
        cnt++;
    }
    printf("result : %d",result);
}

*/

//////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
// 14. Write a C Program to check if two numbers are equal without using the bitwise operator.
//////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

/*

#include <stdio.h>

int main()
{
    int num1=0,num2=0;
    printf("Enter the first no  ");
    scanf("%d",&num1);
    printf("Enter the second no  ");
    scanf("%d",&num2);

    if(!(num1^num2))
        printf("numers are equal.");
    else
        printf("numers are not equal.");
}

*/


//////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
// 15. Write a C Program to find the LCM of two numbers without using any loop or condition.
//////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

/*
#include <stdio.h>

int main() 
{
    int a=0,b=0,lcm=0;
    printf("Enter the first no  ");
    scanf("%d",&a);
    printf("Enter the second no ");
    scanf("%d",&b);

    lcm = (a > b) ? a : b;
    int x=lcm;
    while (1) {
        if (lcm % a == 0 && lcm % b == 0) {
            break;
        }
        lcm *= lcm;
    }

    printf("LCM of %d and %d is %d\n", a, b, lcm);

    return 0;
}

*/

//////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
// 16. Write a C Program to find the Maximum and minimum of two numbers without using any loop or condition.
//////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

/*

#include <stdio.h>
#include <stdlib.h>

int main() {

    int a, b;
    printf("Enter two numbers: ");
    scanf("%d %d", &a, &b);

    int max = (a + b + abs(a - b)) / 2;
    int min = (a + b - abs(a - b)) / 2;

    printf("Maximum: %d\n", max);
    printf("Minimum: %d\n", min);

    return 0;
}

*/


//////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
// 17. Write a Program in C to Print all natural numbers up to N without using a semi-colon.
//////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

/*

#include <stdio.h>
#define N 10

int main(int val)
{
 if (val <= N && printf("%d ", val) && main(val + 1)) {
 }
}

*/


//////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
// 18. Write a Program to create a pyramid pattern using C.
//////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

/*

#include <stdio.h>
#define size  5

int main()
{
    for(int i=1 ; i<= size ; i++)
    {
        for(int j=1 ; j<=(2*size-1) ; j++)
        {
            if(j<=size)
            {
                if((i+j)>size)
                    printf("*");
                else 
                    printf(" ");
            }
            else if(j>size)
            {
                if(i>(j-size))
                    printf("*");
                else 
                    printf(" ");
            }
        }
        printf("\n");
    }
}

*/


//////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
// 19. Write a program to reverse an Array.
//////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

/*

#include <stdio.h>

void reverse(int* arr, int n)
{
    for (int i = 0, j = n - 1; i < j; i++, j--)                  // Concept of multiple variable in for loop
    {
        int ele = arr[i];
        arr[i] = arr[j];
        arr[j] = ele;
    }
}

int main()
{
    int arr[] = { 1, 2, 3, 4, 5 }; 
    reverse(arr, 5);   

    for (int i = 0; i < 5; i++)
        printf("%d ", arr[i]);
}

*/


//////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
// 20. To find the Second Highest Element in the array
//////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

/*
#include <stdio.h>

int main()
{
    int arr[] = {66,72,95,35,5,62,45,68,8,51,23,70,35,61};
    int lar=arr[0],sec_lar=arr[0];

    for(int i=0;i<=(sizeof(arr)/4)-1;i++)
    {
        if(arr[i]>sec_lar)
        {
            if(arr[i]>=lar)
            {
                sec_lar = lar;
                lar = arr[i];
            }
            else
                sec_lar = arr[i];
        }
    }
    printf("Second Largest is : %d",sec_lar);

}

*/


//////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
// 21. Pattern
// *
// #*
// #*#
// *#*#
// *#*#*
// #*#*#*
// #*#*#*#
//////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

/*
#include <stdio.h>

int main()
{
    int a,b=1,flag=1;
    for(int i=1;i<=7;i++)
    {
        for(a=1;a<=b;a++)
        {
            if(flag)
                printf("*");
            else
                printf("#");
            flag ^= 1;
        }
        b++;
        printf("\n");
    }
}
*/


//////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
// 22. Given a string return the first repeating character
//////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////


#include <stdio.h>
#define MAX_LIMIT 20

int main()
{
    char name[MAX_LIMIT];
    fgets(name,MAX_LIMIT,stdin);
    printf("you have entered ->  %s \n",name);
    int size = sizeof(name);

    for(int i=0;i<size-1;i++)
    {
        for(int j=i-1;j>=0;j--)
        {
            if(name[j]==name[i])
            {
                printf("First repeating Character is : %c",name[i]);
                return;
            }
        }
    }
    return 0;
}


//////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
// 23. Take the input from the the user.   ////  fgets() concept 
//////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

/*
#include <stdio.h>
#define MAX_LIMIT 20
int main()
{
   char str[MAX_LIMIT];
   fgets(str, MAX_LIMIT, stdin);                     
   printf("%s", str);

   return 0;
}
*/

//////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
// 24. Divide 2 number without using / operator.
//////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

/*
#include <stdio.h>
#include <math.h>

int main()
{
    int a,b,c;
    printf("Enter the Dividend : ");
    scanf("%d",&a);
    printf("Enter the Divisor : ");
    scanf("%d",&b);
    printf("Enter the upto how much decimal you want answer : ");
    scanf("%d",&c);
    printf("\n");

    ///////////////////////////////////////////  for the Quotient part  ////////////////////////////////////
    int temp=0,cnt=0,quo=0;
    while((a-temp)>=b)
    {
        temp += b;
        cnt++;
    }
    quo=cnt;
    printf("The Quotioent is : %d \n",quo);

    ///////////////////////////////////////////  for the Remainder part  ////////////////////////////////////
    int x=0;
    for(int i=0;i<c;i++)
    {
        a = (a-temp)*10,temp=0,cnt=0;
        while((a-temp)>=b)
        {
            temp += b;
            cnt++;
        }
        x = x*10+cnt;
    }
    printf("The Decimal part is : %d",x);
    printf("\n");
    printf("Final answer is : %d.%d",quo,x);
}

*/
